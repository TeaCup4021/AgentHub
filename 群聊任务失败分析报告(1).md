# 群聊任务失败分析报告

**日期**：2026-06-04  
**问题**：群聊模式下，C语言快速排序任务在 Planner 生成 Plan 并经用户确认后，子任务未被调度执行，Orchestrator 返回空回复

---

## 一、日志时间线

| 时间 | 事件 | 说明 |
|------|------|------|
| 00:10:57 | `POST /messages` (user消息) | 用户发送："帮我写一个C语言快速排序的模板，并放在本机桌面上，然后告诉我每一行代码的意义" |
| 00:10:57 | `INSERT INTO messages` | 用户消息写入，status=`done` |
| 00:10:57 | `INSERT INTO orchestrator_tasks` | 创建编排任务，status=`planning`，plan=`{}` |
| 00:10:57 | `GET /stream?prompt=...&orchestrate_mode=auto_orchestrate` | **第一次 SSE 连接**，携带 prompt（URL编码的中文） |
| 00:11:03 | LiteLLM 警告 | 远程模型价格表获取超时（不影响功能） |
| 00:11:05 | LiteLLM completion | 调用 deepseek-v4-pro 进行规划 |
| 00:11:29 | `Plan JSON parse failed` | ⚠️ Planner 返回的 JSON 解析失败：`Expecting property name enclosed in double quotes: line 1 column 2 (char 1)` |
| 00:11:29 | `INSERT INTO messages` (orchestrator) | 虽然 JSON 解析有警告，但系统仍提取出了 plan（2424字符的规划内容） |
| 00:11:29 | `INSERT INTO artifacts` | Plan artifact 写入，包含 1 个子任务（sub-333ed9c3） |
| 00:11:29 | `UPDATE orchestrator_tasks` | 状态更新为 `plan_draft` |
| 00:12:52 | `GET /messages?limit=50` | 前端拉取消息列表 |
| 00:13:04 | `POST /messages` (confirm_plan) | 用户确认计划 |
| 00:13:04 | `UPDATE orchestrator_tasks` | 状态更新为 `confirmed` |
| 00:13:04 | `INSERT INTO orchestrator_subtasks` | 子任务写入，**status=`queued`**，mode=`single_turn` |
| 00:13:04 | `GET /stream?orchestrate_mode=auto_orchestrate` | **第二次 SSE 连接**，⚠️ **没有 prompt 参数** |
| 00:13:04 | `build_agent: name=代码助手Reimu` | Agent 被构建 |
| 00:13:04 | LiteLLM completion | 调用 deepseek-v4-pro 执行 coordinator |
| 00:13:10 | `INSERT INTO messages` (orchestrator) | Coordinator 返回："I'd be happy to help! However, I don't see any specific task or request in your message..." |
| 00:13:10 | `INSERT INTO messages` (orchestrator summary) | 摘要输出：**成功 0 / 1**，子任务状态仍为 ⬜ queued |
| 00:13:10 | `UPDATE orchestrator_tasks` | 状态被标记为 `completed` |

---

## 二、关键日志摘录

### 2.1 Planner JSON 解析警告（00:11:29）

```
00:11:29 WARNING [agenthub.planner] Plan JSON parse failed:
  Expecting property name enclosed in double quotes: line 1 column 2 (char 1)
```

> 说明：deepseek-v4-pro 模型返回的 JSON 格式不合法（可能包含单引号或非 JSON 内容），虽然系统通过容错机制提取出了 plan，但这是模型在结构化输出方面的不稳定信号。

### 2.2 Plan 确认与子任务入队（00:13:04）

```
UPDATE orchestrator_tasks SET status='confirmed', result_summary='{"state_delta": {"plan_confirmed": true}}'

INSERT INTO orchestrator_subtasks (task_id, agent_id, instruction, status, ...)
VALUES (..., '帮我写一个C语言快速排序的模板，并放在本机桌面上，然后告诉我每一行代码的意义', 'queued', ..., 'single_turn', ...)
```

> 子任务被创建并设为 `queued` 状态，这是它最后一次出现——之后再也没有被更新为 `running`。

### 2.3 第二次 SSE 连接缺少 prompt（00:13:04）

```
# 第一次 SSE（规划阶段）—— 正常携带 prompt：
GET /api/v1/conversations/.../stream?prompt=%E5%B8%AE%E6%88%91%E5%86%99...&orchestrate_mode=auto_orchestrate HTTP/1.1

# 第二次 SSE（确认后执行阶段）—— prompt 缺失：
GET /api/v1/conversations/.../stream?orchestrate_mode=auto_orchestrate HTTP/1.1
```

### 2.4 Coordinator 空回复（00:13:10）

```
INSERT INTO messages (sender_type='agent', content='text',
  content='I\'d be happy to help! However, I don\'t see any specific task or request in your message.
Could you please describe what you\'d like me to do?...',
  meta='{"agent_name": "orchestrator"}')
```

### 2.5 执行摘要确认失败（00:13:10）

```
### 各 Agent 输出
| Agent | 状态 | 耗时 | 摘要 |
|-------|------|------|------|
| single_turn | ⬜ | - |  |

成功: 0 / 1
```

---

## 三、根因分析

### 🔴 根因：前端确认计划后重连 SSE 时丢失 prompt 参数

**调用链追踪：**

```
用户确认 Plan
  → ChatArea.tsx handleConfirmPlan()
    → createSSEStream(activeId, callbacks, undefined, "auto_orchestrate", ...)
                                                        ^^^^^^^^^
                                                        prompt = undefined
      → sse.ts: if (prompt) ... → false，不拼接到 URL
        → GET /stream?orchestrate_mode=auto_orchestrate  (无 prompt)

后端 stream_conversation() 收到 prompt=None
  → _coordinator_stream(conv_id, user_id, prompt=None, ...)
    → runner.stream_single_chat(message=prompt or "")  → message=""
      → Coordinator LLM 收到空消息
        → 返回 "I don't see any specific task..."
```

**代码证据 1** — 前端 `ChatArea.tsx:468-475`（Bug 所在）：

```typescript
const handleConfirmPlan = useCallback((planId: string, subtasks: PlanSubtask[]) => {
    // ...
    messageApi.send(activeId, { content: "", mode: "confirm_plan", plan_id: planId, plan }).then(() => {
      // ...
      disconnectRef.current = createSSEStream(activeId, {
        ...callbacks,
        onConnectionError: () => { /* ... */ },
      }, undefined, "auto_orchestrate", plannerAgentIdRef.current);
      // ^^^^^^^^^ prompt 传了 undefined
    })
```

**对比** — 同一文件中正常发送消息的代码（`ChatArea.tsx:385-388`）正确传入了 `content`：

```typescript
disconnectRef.current = createSSEStream(convId, {
    ...buildCallbacks(convId, conv),
    onConnectionError,
}, content, streamMode, plannerAgentIdRef.current);
// ^^^^^^^ 这里有 prompt
```

**代码证据 2** — `sse.ts:54`，prompt 为空时不拼接到 URL：

```typescript
if (prompt) streamUrl.searchParams.set("prompt", prompt);
```

**代码证据 3** — 后端 `conversations.py:859`，prompt 为空时传入空字符串：

```python
runner.stream_single_chat(
    user_id=str(user_id),
    session_id=str(conv_id),
    message=prompt or "",  # prompt=None → message=""
),
```

### 🟡 次要问题：后端未从 Plan 中注入子任务指令

`_coordinator_stream` 从 `orch_task.plan` 中只提取了 `agent_ids`，完全忽略了每个 subtask 的 `instruction` 字段：

```python
# conversations.py:828-830 — 当前代码
plan = orch_task.plan or {}
subtasks = plan.get("subtasks", [])
agent_ids = [UUID(st["agent_id"]) for st in subtasks if st.get("agent_id")]
# ← instruction 字段被忽略了！

实际 Plan 中的 subtask：
{
  "subtask_id": "sub-333ed9c3",
  "agent_id": "69da33ae-45d5-41f9-9e03-f1718aed9ea6",
  "agent_name": "代码助手Reimu",
  "instruction": "帮我写一个C语言快速排序的模板，并放在本机桌面上，然后告诉我每一行代码的意义",
  "depends_on": [],
  "mode": "single_turn"
}
```

Coordinator 的 instruction（`coordinator_builder.py:133-153`）只列举了 Agent 的能力描述，不包含具体任务：

```python
def _build_coordinator_instruction(self, agents: list[AgentModel]) -> str:
    agent_descriptions = "\n".join(
        f"- {a.name} (call: request_task_{self._sanitize_name(a.name)}): "
        f"{a.system_prompt[:150] if a.system_prompt else 'No description'}"
        for a in agents
    )
    return (
        "You are an intelligent orchestrator. Your job is to understand the "
        "user's request and coordinate the appropriate specialists...\n\n"
        f"Available specialists:\n{agent_descriptions}\n\n"
        # ← 没有注入 Plan 中的具体任务描述！
    )
```

### 🟡 次要问题：子任务状态从未被更新

子任务在 `confirm_plan` 时被插入为 `queued`，只有在 `_update_subtask_metrics()` 中才会被更新（`conversations.py:878-884`）。由于 coordinator 没有真正派发任务，tracer 中无记录，子任务永远停留在 `queued` 状态。

### 🟡 Phase 2 路由逻辑：`planner_agent_id` 决定执行模式导致 Plan 被丢弃

**文件**：`backend/app/api/v1/conversations.py:1155-1167`

当前 Phase 2 的路由逻辑：

```python
if confirmed_task.planner_agent_id:
    # Agent-planned → DAG mode
    return StreamingResponse(_dag_workflow_stream(...))
else:
    # LLM-planned → Coordinator mode
    return StreamingResponse(_coordinator_stream(...))
```

**问题**：DAG 模式完整使用 Plan 中的 subtask instruction；Coordinator 模式完全丢弃它。两种 Planner 产生的 Plan 结构完全相同，但执行时一个被忠实执行、一个被忽略。

#### 两种模式对 Plan 的使用差异

```
                     DAG 模式                           Coordinator 模式
                     ──────────                         ────────────────
Plan 使用方式:    ✅ 完整读取每个 subtask:             ❌ 只提取 agent_ids:
                    instruction → 注入 Agent 节点         instruction 字段被丢弃

Agent 收到的:     DB system_prompt                     Coordinator: 通用编排模板
                  + "Your specific task: 帮我写..."     Sub-Agent: 原始 system_prompt
                                                       (无任务上下文!)

prompt 依赖:      ✅ 不依赖 prompt                     ❌ 完全依赖 prompt
```

#### 代码对比

**DAG 模式** — 完整使用 Plan：

```python
# _dag_workflow_stream:923-934
plan_obj = OrchestratorPlan(subtasks=[
    SubTaskPlan(
        instruction=st["instruction"],  # ← 读取并传递
        ...
    )
    for st in subtasks
])

# WorkflowBuilder._merge_instruction:103-109
instruction = db_agent.system_prompt + "\n\nYour specific task: " + task_instruction
# → "你是一个C语言专家...\n\nYour specific task: 帮我写C语言快速排序模板..."
```

**Coordinator 模式** — 完全丢弃 Plan：

```python
# _coordinator_stream:828-830
plan = orch_task.plan or {}
subtasks = plan.get("subtasks", [])
agent_ids = [UUID(st["agent_id"]) for st in subtasks if st.get("agent_id")]
# ← instruction 字段被完全忽略！

# Coordinator 收到的指令（CoordinatorBuilder:133-153）:
"You are an intelligent orchestrator. Your job is to understand the "
"user's request and coordinate the appropriate specialists...\n\n"
f"Available specialists:\n{agent_descriptions}"
# ← 没有任何具体任务描述！
```

#### 本次日志中的实际影响

```
实际路径: planner_agent_id=None → Coordinator 模式
  → Coordinator 收到空 prompt + 无任务上下文 → "I don't see any task" ❌

假设路径: 走 DAG 模式
  → subtask instruction 注入 Agent 节点 → Agent 收到完整任务 → 执行成功 ✅
```

**路由判断依据 `planner_agent_id` 是否为空的逻辑是错误的**。两种 Planner（Agent 规划 vs LLM 规划）产出的 Plan 结构完全相同，都包含带 `instruction` 的有效 subtask。正确的路由应该是：Plan 中存在有效 subtask 就应走 DAG 模式忠实执行。

### 🟡 路由分支盲区：`stream_conversation` 状态机未覆盖 `completed` 状态

**文件**：`backend/app/api/v1/conversations.py:1098-1200`

`stream_conversation` 端点是一个基于 `OrchestratorTask.status` 的状态机路由。它目前只显式处理了四种状态：

```
stream_conversation() 路由状态机
│
├─ Phase 1:   status='planning'    → _orchestrator_plan_stream()      (line 1108)
├─ Phase 1.5: status='refining'    → _orchestrator_refine_stream()    (line 1126)
├─ Phase 2:   status='confirmed'   → _coordinator / _dag_workflow     (line 1144)
│
├─ 兜底: orchestrate_mode=="auto_orchestrate"                         (line 1171)
│   ├─ status='failed'  → PLANNER_ERROR                               (line 1176)
│   └─ 无任何 task      → NO_ORCHESTRATOR_TASK   ← LINE 1193          (line 1188)
│
└─ 最终兜底: 单 Agent 聊天模式                                        (line 1202)
```

**Line 1193 代码**：

```python
# 1188-1200
any_task = await db.execute(
    select(OrchestratorTask).where(
        OrchestratorTask.conversation_id == conv_id,
    ).limit(1)
)
if not any_task.scalar_one_or_none():   # ← 1193: task 不存在才报错
    return StreamingResponse(
        _error_sse_stream(
            "NO_ORCHESTRATOR_TASK",
            "群聊模式需要先创建编排任务，请检查对话是否已绑定 Agent 后重试",
        ),
        media_type="text/event-stream",
    )
```

**问题**：`completed` 状态形成一个覆盖盲区。

当 `_coordinator_stream` / `_dag_workflow_stream` 执行完毕并将 task 状态改为 `completed` 后，如果前端因某些原因重连 SSE，路由机将无法匹配到任何显式分支：

```
重连时的匹配过程：
├─ status='planning'?   → NO（是 completed）
├─ status='refining'?   → NO
├─ status='confirmed'?  → NO（已被改成 completed）← 关键盲区
├─ status='failed'?     → NO
└─ any_task?            → YES（completed task 存在）
    → line 1193 为 False，不触发错误
    → 静默落入"单 Agent 聊天模式"！   ← BUG
```

**在本次日志中的表现**：本次失败的第二 SSE 连接 **正确命中了 Phase 2**（`confirmed` 状态），所以 line 1193 的盲区不是本次失败的直接原因。但它是**同一条路由函数内的设计缺陷**——`completed` 状态的 task 会被静默回退到单 Agent 模式，前端无法感知到这是「任务已执行完毕」还是「路由出错」。

**修复**：在 Phase 2 之后、兜底之前，增加对 `completed` 状态的显式处理：

```python
# Phase 2.5: Task already completed — return cached summary
result = await db.execute(
    select(OrchestratorTask)
    .where(
        OrchestratorTask.conversation_id == conv_id,
        OrchestratorTask.status == "completed",
    )
    .order_by(OrchestratorTask.created_at.desc())
    .limit(1)
)
completed_task = result.scalar_one_or_none()
if completed_task:
    return StreamingResponse(
        _replay_completed_stream(conv_id, completed_task, db),
        media_type="text/event-stream",
    )
```

或者在 `orchestrate_mode == "auto_orchestrate"` 的兜底分支中，将 `completed` 也纳入检查范围并返回有意义的响应，而不是静默回退单 Agent 模式。

### 🔵 回调触发验证：DAG 模式 vs Coordinator 模式

有人提出：Coordinator 模式下 `before/after_agent_callback` 正常触发，但 DAG 模式（`Runner(node=workflow)`）下 ADK Workflow Graph 内部节点不走标准 Agent 回调路径，导致 `tracer.records` 始终为空。

**验证结论：这个说法不成立。** DAG 模式和 Coordinator 模式的回调触发路径完全一致。

#### 完整调用链对比

```
Coordinator 模式:                          DAG 模式:
───────────────                            ─────────
Runner.run_async()                         Runner.run_async()
  → _run_node_async()                        → _run_node_async()
    → DynamicNodeScheduler                     → NodeRunner.run()
      → NodeRunner.run()                         → BaseNode.run()
        → BaseNode.run()                           → _run_impl()        ← 同一入口
          → _run_impl()                              → run_async()      ← 同一方法
            → run_async()                              → _handle_before_agent_callback() ✅
              → _handle_before_agent_callback() ✅      → _handle_after_agent_callback()  ✅
              → _handle_after_agent_callback()  ✅
```

#### 源代码证据

**第 1 步** — `Runner.__init__` 无论 `agent` 还是 `node`，都包装为 `App.root_agent`（`_resolve_app` 方法）：

```python
if agent is not None:
    return App(name=app_name, root_agent=agent, ...)
if node is not None:
    return App(name=..., root_agent=node, ...)     # Workflow 成为 root_agent
```

**第 2 步** — `Runner.run_async()` 路由，两种模式都进入 `_run_node_async()`：

```python
if isinstance(self.agent, LlmAgent):
    agent_to_run = build_node(agent_to_run)
    # → _run_node_async(node=agent_to_run)    # Coordinator 走这里

elif isinstance(self.agent, BaseNode) and not isinstance(self.agent, BaseAgent):
    # → _run_node_async()                     # Workflow 走这里
```

**第 3 步** — Workflow 内部为每个子节点创建 `NodeRunner`（`_workflow.py:_start_node_task`）：

```python
runner = NodeRunner(node=node, parent_ctx=ctx, ...)
loop_state.pending_tasks[node_name] = asyncio.create_task(
    runner.run(node_input=trigger.input, ...)
)
```

**第 4 步** — `NodeRunner.run()` 调用 `BaseNode.run()`（`_node_runner.py:261`）：

```python
async with Aclosing(self._node.run(ctx=ctx, node_input=node_input)) as agen:
```

**第 5 步** — `BaseNode.run()` 调用 `_run_impl()`（`_base_node.py:215`）：

```python
async with Aclosing(self._run_impl(ctx=ctx, node_input=node_input)) as agen:
```

**第 6 步** — `LlmAgent._run_impl()` 就是 `BaseAgent._run_impl()`，它直接调用 `run_async()`（`base_agent.py:289-298`）：

```python
@override
async def _run_impl(self, *, ctx: Context, node_input: Any):
    """Runs the agent as a node."""
    async for event in self.run_async(
        parent_context=ctx.get_invocation_context()
    ):
```

**第 7 步** — `run_async()` 触发回调（`base_agent.py:257-286`）：

```python
async def run_async(self, parent_context):
    ctx = self._create_invocation_context(parent_context)
    if event := await self._handle_before_agent_callback(ctx):  # ← ✅
        yield event
    ...
    if event := await self._handle_after_agent_callback(ctx):   # ← ✅
        yield event
```

#### 结论

两条路径最终都汇入 `BaseAgent.run_async()` → `_handle_before/after_agent_callback()`。`ExecutionTracer` 的回调在两种模式下都会触发。如果实际观察到 `tracer.records` 为空，更可能的原因是：

1. **DAG 模式从未被实际走到** — `planner_agent_id` 始终为 None，路由一直走 Coordinator 分支
2. **只测试了 Coordinator 模式** — `_dag_workflow_stream` 从未被触发

---

## 四、修复方案

### 修复 1（前端 — 直接修复）

**文件**：`agenthub-web/src/components/layout/ChatArea.tsx:475`

```diff
- }, undefined, "auto_orchestrate", plannerAgentIdRef.current);
+ }, lastPromptRef.current, "auto_orchestrate", plannerAgentIdRef.current);
```

`lastPromptRef` 在每次发送消息时已存入原始 prompt（`ChatArea.tsx:353`：`lastPromptRef.current = content`），直接复用即可。

### 修复 2（后端 — Phase 2 路由修正）

**文件**：`backend/app/api/v1/conversations.py:1155-1167`

当前按 `planner_agent_id` 是否为空决定 DAG 还是 Coordinator 的路由逻辑存在缺陷。LLM-planned 同样产出了带 `instruction` 的有效 subtask，应走 DAG 模式忠实执行。

**方案 A**：统一走 DAG 模式（推荐）

```python
if confirmed_task:
    # Plan 中存在有效 subtask → 走 DAG 模式忠实执行
    return StreamingResponse(
        _dag_workflow_stream(conv_id, user_id, prompt, confirmed_task, db),
        media_type="text/event-stream",
    )
```

**方案 B**：退而求其次，在 Coordinator 模式中注入 Plan 上下文

```python
else:
    # Coordinator 模式：注入 Plan 中的 subtask 指令作为上下文
    instructions = "\n".join(
        f"- {st.get('agent_name', 'Unknown')}: {st.get('instruction', '')}"
        for st in subtasks
    )
    context_message = f"需要完成以下任务:\n{instructions}\n\n用户请求: {prompt or '见任务描述'}"
    return StreamingResponse(
        _coordinator_stream(conv_id, user_id, context_message, confirmed_task, db),
        media_type="text/event-stream",
    )
```

### 修复 3（后端 — 兜底修复）

**文件**：`backend/app/api/v1/conversations.py`，`_coordinator_stream` 函数

在构建 coordinator 消息时，将 plan 中 subtask 的 instruction 作为上下文注入：

```python
# 方案 A：将 subtask 指令拼入 prompt
plan = orch_task.plan or {}
subtasks = plan.get("subtasks", [])
instructions = "\n".join(
    f"- {st.get('agent_name', 'Unknown')}: {st.get('instruction', '')}"
    for st in subtasks
)
context_message = f"需要完成以下任务:\n{instructions}\n\n用户原始请求: {prompt or '见任务描述'}"
```

或

**方案 B**：修改 `CoordinatorBuilder._build_coordinator_instruction` 接收并注入 subtask 指令。

### 修复 3（后端 — 状态一致性）

在 `_coordinator_stream` / `_dag_workflow_stream` 开始时，主动将子任务状态从 `queued` 更新为 `running`，避免出现"已完成但子任务仍为 queued"的不一致状态。

---

## 五、影响范围

| 影响 | 描述 |
|------|------|
| 阻塞功能 | 所有使用群聊模式（auto_orchestrate）的用户，在确认 Plan 后均无法正常执行任务 |
| 触发条件 | orchestrator 模式为 LLM-planned（`planner_agent_id` 为空），走 `_coordinator_stream` 分支 |
| 影响平台 | 前端 + 后端均需修复 |
| 数据影响 | `orchestrator_subtasks` 表中残留状态为 `queued` 的孤立子任务 |
| 路由盲区 | `completed` 状态的 task 在 SSE 重连时静默回退单 Agent 聊天模式 |
