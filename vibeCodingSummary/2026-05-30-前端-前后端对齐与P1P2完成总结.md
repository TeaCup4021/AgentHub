# AgentHub 前端 — 前后端对齐 P1/P2 完整实现总结

**日期**: 2026-05-30 | **实施**: 前端全栈（Mock + API + Store + Hook + UI）

---

## 功能概述

基于后端 6 个新提交 (e6b16ce..309978c)，完成前后端全量对齐。后端新增认证、项目、Agent 能力、编排器 DAG 等模块，前端完成 API 类型/调用/Hook/Store/Mock/UI 全链路覆盖，外加 4 项体验修复。

`npx tsc -b --noEmit` 零错误，`npx vitest run` 86/86 全部通过。

---

## P1 核心对齐（T1-T6）

### T1 — 对话与项目关联

| 文件 | 改动 |
|------|------|
| `types/chat.ts` | `Conversation` / `CreateConversationParams` 加 `projectId?: string` |
| `types/api.ts` | `ConversationListParams` 加 `projectId?: string` |
| `lib/api.ts` | `conversationApi.create` 和 `list` 透传 `projectId` |
| `hooks/useConversations.ts` | 新增 `useConversationsByProject(projectId?)` |
| `mocks/data.ts` | mock Conversation 加入 `projectId` |
| `mocks/handlers.ts` | GET 支持 projectId/keyword 组合过滤；POST 存储 projectId |

### T2 — Agent 能力标签

| 文件 | 改动 |
|------|------|
| `lib/api.ts` | `agentApi` 新增 `capabilities()` |
| `hooks/useAgents.ts` | 新增 `useAgentCapabilities()`（staleTime 60s） |
| `mocks/handlers.ts` | Mock `GET /agents/capabilities` → 9 种能力 |

### T3 — 认证 UI 增强

| 文件 | 改动 |
|------|------|
| `components/layout/IconSidebar.tsx` | 底部用户头像 + Dropdown（显示邮箱、退出登录） |

### T4 — 密码修改 UI

| 文件 | 改动 |
|------|------|
| `lib/api.ts` | 新增 `authApi.changePassword(oldPassword, newPassword)` |
| `components/settings/SettingsPage.tsx` | 新增"安全"导航项 + 密码修改表单（含客户端确认密码校验） |
| `mocks/handlers.ts` | Mock `PATCH /auth/password` — 新旧相同拦截，正确验证放行 |

### T5 — 项目管理全链路

**新增文件：**

| 文件 | 说明 |
|------|------|
| `types/project.ts` | `Project` / `CreateProjectParams` / `UpdateProjectParams` |
| `hooks/useProjects.ts` | `useProjects` / `useProject` / `useCreateProject` / `useUpdateProject` / `useDeleteProject` |
| `components/project/ProjectSwitcher.tsx` | 侧边栏项目选择器（IconSidebar 卡片入口） |
| `components/project/ProjectCreateModal.tsx` | 新建项目弹窗（Semi Form） |

**修改文件：**

| 文件 | 改动 |
|------|------|
| `types/index.ts` + `types/api.ts` | 导出 + 6 个 Project API 类型别名 |
| `lib/api.ts` | `projectApi` (create/list/detail/update/delete，5 个方法) |
| `hooks/index.ts` | 导出 project hooks |
| `stores/uiStore.ts` | `selectedProjectId` + `setSelectedProjectId`（localStorage 持久化） |
| `components/layout/AppLayout.tsx` | 读 `selectedProjectId` → 传 `useConversations({ projectId })` |
| `components/layout/ConversationList.tsx` | 不直接改动——通过 AppLayout 传递过滤后的数据 |
| `mocks/data.ts` | 2 个 mock 项目（proj-1 / proj-2） |
| `mocks/handlers.ts` | 5 个 CRUD mock 端点 |

### T6 — Mock 补齐

**Mock 新增端点：**

| 端点 | 行为 |
|------|------|
| `POST /v1/auth/send-code` | 生成 6 位验证码，60 秒限频，10 分钟过期 |
| `POST /v1/auth/register` | 验证码校验 → 邮箱唯一性 → 注册 → TokenResponse |
| `POST /v1/auth/login` | 邮箱+密码验证 → TokenResponse |
| `POST /v1/auth/refresh` | 返回新 TokenResponse |
| `GET /v1/auth/me` | 返回当前用户信息 |
| `GET /v1/orchestrator/tasks/{id}/dag` | 3 节点 2 边示例 DAG |

**预设账号：** `test@agenthub.dev` / `123456`

---

## P2 差异化亮点（T7-T9）

### T7 — DAG 可视化面板

| 文件 | 改动 |
|------|------|
| `types/chat.ts` | 新增 `DagNode` / `DagEdge` / `DagResponse` |
| `lib/api.ts` | 新增 `orchestratorApi.dag(taskId)` |
| `stores/chatStore.ts` | `dagTaskId` / `setDagTaskId` |
| `components/chat/DagGraph.tsx` | **新建** — SVG 拓扑图，分层布局 + 贝塞尔箭头 + status 着色 |
| `components/chat/ReActPanel.tsx` | 新增 ReAct / 任务图 双 Tab 切换 |

### T8 — SSE orchestrateMode 对接

| 文件 | 改动 |
|------|------|
| `lib/sse.ts` | `createSSEStream` 新增 `orchestrateMode` 参数 → `?orchestrate_mode=` |
| `components/layout/ChatArea.tsx` | 群聊传递 `auto_orchestrate`；`onAgentStatus` 捕获 `task_id` → `setDagTaskId`；对话切换清除 `dagTaskId` |
| `mocks/sse.ts` | 4 个 `agent_status` 事件全加 `task_id` |

### T9 — 消息过滤参数

| 文件 | 改动 |
|------|------|
| `lib/api.ts` | `messageApi.list` 新增可选 `senderType?` / `senderId?` |
| `mocks/handlers.ts` | mock 支持 senderType/senderId 过滤 |

---

## 体验修复（4 项）

| # | 问题 | 修复文件 | 方案 |
|---|------|------|------|
| 1 | 设置页左侧导航跟随滚动 | `components/settings/SettingsPage.tsx` | 外层 `minHeight: 0` |
| 2 | 项目管理 UI 隐蔽、风格不统一 | `components/layout/IconSidebar.tsx` | 项目入口移至 IconSidebar 中部，卡片式视觉 |
| 3 | 返回主页弹新建对话弹窗 | `stores/uiStore.ts` + `ConversationList.tsx` | `resetNewConvTrigger()` 消费后清零 |
| 4 | 无用户信息编辑 | `lib/api.ts` + `SettingsPage.tsx` + `mocks/handlers.ts` | 新增 `authApi.updateProfile()` + 个人信息编辑表单 + Mock |

---

## 文件统计

| 类别 | 新增 | 修改 | 总计 |
|------|:--:|:--:|:--:|
| types | 1 (project.ts) | 3 (chat/api/index) | 4 |
| api | — | 1 | 1 |
| hooks | 1 (useProjects.ts) | 5 (index + useConversations/useAgents + 2 plan) | 6 |
| stores | — | 2 (uiStore/chatStore) | 2 |
| components | 3 (ProjectSwitcher/ProjectCreateModal/DagGraph) | 5 (IconSidebar/SettingsPage/ConversationList/AppLayout/ChatArea) | 8 |
| sse/lib | — | 2 (sse.ts/api.ts) | 2 |
| mocks | — | 2 (handlers/data + sse) | 3 |
| docs | 4 | 2 | 6 |
| **总计** | **9** | **22** | **31** |

---

## 与后端对齐状态

- **34 个后端端点，33 个已对齐覆盖**（含前端 Mock + API + Hook + UI 四层）
- **1 个缺口**：`PATCH /v1/auth/me` — 前端已实现（API + Mock + UI），待后端新增 schema/service/route
- 详细对齐矩阵见 `agenthub-web/docs/specs/2026-05-30-frontend-backend-alignment-gaps.md`

---

## 验证

- `npx tsc -b --noEmit`：零错误
- `npx vitest run`：14 文件 86 tests 全部通过
- Mock 模式群聊：SSE event → agent_status task_id → chatStore → ReActPanel DAG 视图 → SVG 拓扑图渲染，端到端可演示
