# 项目管理功能 - 后端设计方案

**日期**: 2026-05-29 | **版本**: v1.0

---

## 一、需求概述

为用户提供**项目（Project）**维度来组织对话。用户可以创建多个项目，将对话归属到不同项目下，实现工作区的隔离和聚焦。

核心能力：
- 项目 CRUD（创建、列表、编辑、删除）
- 对话关联项目（新建/已有对话可归属到项目）
- 按项目过滤对话列表
- 项目可预设默认 Agent 列表

---

## 二、数据库变更

### 2.1 新增 `projects` 表

```sql
CREATE TABLE projects (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    name VARCHAR(200) NOT NULL,
    description TEXT,
    owner_id UUID NOT NULL REFERENCES users(id),
    default_agent_ids UUID[] DEFAULT '{}',
    created_at TIMESTAMPTZ DEFAULT now(),
    updated_at TIMESTAMPTZ DEFAULT now()
);
```

### 2.2 `conversations` 表新增字段

```sql
ALTER TABLE conversations ADD COLUMN project_id UUID REFERENCES projects(id) ON DELETE SET NULL;
-- 允许 NULL，兼容存量数据；删除项目时对话保留，project_id 置空
```

### 2.3 迁移文件

新建 `backend/alembic/versions/0005_add_projects.py`，依赖 `0004`。

```python
# 核心操作：
# upgrade: 创建 projects 表 + conversations 加 project_id 列
# downgrade: 删除 project_id 列 + 删除 projects 表
```

---

## 三、模型层

### 3.1 新增模型 `backend/app/models/project.py`

```python
import uuid
from sqlalchemy.orm import Mapped, mapped_column
from sqlalchemy import String, Text, ForeignKey
from sqlalchemy.dialects.postgresql import UUID, ARRAY
from app.models.base import Base, UUIDMixin, TimestampMixin


class Project(Base, UUIDMixin, TimestampMixin):
    __tablename__ = "projects"

    name: Mapped[str] = mapped_column(String(200), nullable=False)
    description: Mapped[str | None] = mapped_column(Text, nullable=True)
    owner_id: Mapped[uuid.UUID] = mapped_column(
        UUID(as_uuid=True), ForeignKey("users.id"), nullable=False
    )
    default_agent_ids: Mapped[list[uuid.UUID]] = mapped_column(
        ARRAY(UUID(as_uuid=True)), default=list, server_default="{}"
    )
```

遵循现有模式：继承 `Base, UUIDMixin, TimestampMixin`。

### 3.2 修改模型 `backend/app/models/conversation.py`

新增一个字段：

```python
project_id: Mapped[uuid.UUID | None] = mapped_column(
    UUID(as_uuid=True), ForeignKey("projects.id", ondelete="SET NULL"), nullable=True
)
```

### 3.3 更新 `backend/app/models/__init__.py`

```python
from .project import Project  # 新增
```

---

## 四、Schema 层

### 4.1 新增 `backend/app/schemas/project.py`

```python
from uuid import UUID
from datetime import datetime
from typing import Optional, List
from app.schemas.base import BaseSchema


class ProjectCreate(BaseSchema):
    name: str
    description: Optional[str] = None
    default_agent_ids: List[UUID] = []


class ProjectUpdate(BaseSchema):
    name: Optional[str] = None
    description: Optional[str] = None
    default_agent_ids: Optional[List[UUID]] = None


class ProjectResponse(BaseSchema):
    id: UUID
    name: str
    description: Optional[str] = None
    owner_id: UUID
    default_agent_ids: List[UUID]
    created_at: datetime
    updated_at: datetime


class ProjectListResponse(BaseSchema):
    """用于列表场景的精简响应（不含 default_agent_ids 详情）"""
    id: UUID
    name: str
    description: Optional[str] = None
    conversation_count: int  # 该项目下的对话数量
    created_at: datetime
    updated_at: datetime
```

### 4.2 修改 `backend/app/schemas/conversation.py`

`ConversationCreate` 新增可选字段：

```python
class ConversationCreate(BaseSchema):
    title: str
    type: str = "single"
    agent_ids: List[UUID] = []
    project_id: Optional[UUID] = None  # 新增
```

`ConversationResponse` 新增字段：

```python
class ConversationResponse(BaseSchema):
    # ... 现有字段 ...
    project_id: Optional[UUID] = None  # 新增
```

---

## 五、服务层

### 5.1 新增 `backend/app/services/project.py`

```python
from uuid import UUID
from typing import Optional
from sqlalchemy.ext.asyncio import AsyncSession
from sqlalchemy import select, func, desc
from app.models.project import Project
from app.models.conversation import Conversation
from app.schemas.project import ProjectCreate, ProjectUpdate
from app.schemas.base import Page
from fastapi import HTTPException


class ProjectService:

    @staticmethod
    async def create_project(
        db: AsyncSession, user_id: UUID, data: ProjectCreate
    ) -> dict:
        project = Project(
            name=data.name,
            description=data.description,
            owner_id=user_id,
            default_agent_ids=data.default_agent_ids or [],
        )
        db.add(project)
        await db.commit()
        await db.refresh(project)
        return ProjectService._to_response(project, conversation_count=0)

    @staticmethod
    async def list_projects(
        db: AsyncSession, user_id: UUID
    ) -> list[dict]:
        """列出用户的所有项目，附带每个项目的对话数量"""
        query = (
            select(
                Project,
                func.count(Conversation.id).label("conv_count"),
            )
            .outerjoin(
                Conversation,
                (Conversation.project_id == Project.id)
                & (Conversation.is_deleted == False),
            )
            .where(Project.owner_id == user_id)
            .group_by(Project.id)
            .order_by(desc(Project.updated_at))
        )
        result = await db.execute(query)
        rows = result.all()

        return [
            {
                "id": row.Project.id,
                "name": row.Project.name,
                "description": row.Project.description,
                "default_agent_ids": row.Project.default_agent_ids or [],
                "conversation_count": row.conv_count,
                "created_at": row.Project.created_at,
                "updated_at": row.Project.updated_at,
            }
            for row in rows
        ]

    @staticmethod
    async def get_project(
        db: AsyncSession, user_id: UUID, project_id: UUID
    ) -> dict:
        project = await ProjectService._get_owned(db, user_id, project_id)

        # 统计对话数
        count_result = await db.execute(
            select(func.count(Conversation.id)).where(
                Conversation.project_id == project_id,
                Conversation.is_deleted == False,
            )
        )
        conv_count = count_result.scalar_one_or_none() or 0

        return ProjectService._to_response(project, conversation_count=conv_count)

    @staticmethod
    async def update_project(
        db: AsyncSession, user_id: UUID, project_id: UUID, data: ProjectUpdate
    ) -> dict:
        project = await ProjectService._get_owned(db, user_id, project_id)

        if data.name is not None:
            project.name = data.name
        if data.description is not None:
            project.description = data.description
        if data.default_agent_ids is not None:
            project.default_agent_ids = data.default_agent_ids

        await db.commit()
        await db.refresh(project)

        # 重新统计
        count_result = await db.execute(
            select(func.count(Conversation.id)).where(
                Conversation.project_id == project_id,
                Conversation.is_deleted == False,
            )
        )
        conv_count = count_result.scalar_one_or_none() or 0
        return ProjectService._to_response(project, conversation_count=conv_count)

    @staticmethod
    async def delete_project(
        db: AsyncSession, user_id: UUID, project_id: UUID
    ) -> None:
        project = await ProjectService._get_owned(db, user_id, project_id)
        # 将关联对话的 project_id 置空（由 ON DELETE SET NULL 保证）
        await db.delete(project)
        await db.commit()

    # ---- helpers ----

    @staticmethod
    async def _get_owned(db: AsyncSession, user_id: UUID, project_id: UUID) -> Project:
        result = await db.execute(
            select(Project).where(
                Project.id == project_id, Project.owner_id == user_id
            )
        )
        project = result.scalar_one_or_none()
        if not project:
            raise HTTPException(status_code=404, detail="Project not found")
        return project

    @staticmethod
    def _to_response(project: Project, conversation_count: int) -> dict:
        return {
            "id": project.id,
            "name": project.name,
            "description": project.description,
            "owner_id": project.owner_id,
            "default_agent_ids": project.default_agent_ids or [],
            "conversation_count": conversation_count,
            "created_at": project.created_at,
            "updated_at": project.updated_at,
        }
```

### 5.2 修改 `backend/app/services/conversation.py`

**`list_conversations`** 新增 `project_id` 过滤参数：

```python
@staticmethod
async def list_conversations(
    db: AsyncSession,
    user_id: UUID,
    page: int = 1,
    page_size: int = 10,
    keyword: Optional[str] = None,
    project_id: Optional[UUID] = None,  # 新增
) -> Page:
    query = select(Conversation).where(
        Conversation.owner_id == user_id,
        Conversation.is_deleted == False,
    )
    if project_id:
        query = query.where(Conversation.project_id == project_id)
    # ... 其余逻辑不变
```

**`create_conversation`** 接收 `project_id`：

```python
@staticmethod
async def create_conversation(
    db: AsyncSession, user_id: UUID, data: ConversationCreate
):
    new_conv = Conversation(
        title=data.title,
        type=data.type,
        owner_id=user_id,
        project_id=data.project_id,  # 新增
        last_active_at=datetime.now(timezone.utc),
    )
    # ... 其余逻辑不变
```

**`get_conversation`** 和响应构建中增加 `project_id`：

```python
return {
    # ... 现有字段 ...
    "project_id": conv.project_id,  # 新增
}
```

---

## 六、API 路由层

### 6.1 新增 `backend/app/api/v1/projects.py`

```python
from uuid import UUID
from fastapi import APIRouter, Depends, status
from sqlalchemy.ext.asyncio import AsyncSession

from app.core.database import get_db
from app.api.deps import get_current_user_id
from app.schemas.project import ProjectCreate, ProjectUpdate, ProjectResponse
from app.services.project import ProjectService

router = APIRouter()


@router.post("", response_model=ProjectResponse, status_code=status.HTTP_201_CREATED)
async def create_project(
    data: ProjectCreate,
    db: AsyncSession = Depends(get_db),
    user_id: UUID = Depends(get_current_user_id),
):
    return await ProjectService.create_project(db=db, user_id=user_id, data=data)


@router.get("")
async def list_projects(
    db: AsyncSession = Depends(get_db),
    user_id: UUID = Depends(get_current_user_id),
):
    """返回用户所有项目，附带每个项目的对话数量"""
    items = await ProjectService.list_projects(db=db, user_id=user_id)
    return items  # 中间件自动包裹为 {code, data, message}


@router.get("/{project_id}", response_model=ProjectResponse)
async def get_project(
    project_id: UUID,
    db: AsyncSession = Depends(get_db),
    user_id: UUID = Depends(get_current_user_id),
):
    return await ProjectService.get_project(db=db, user_id=user_id, project_id=project_id)


@router.patch("/{project_id}", response_model=ProjectResponse)
async def update_project(
    project_id: UUID,
    data: ProjectUpdate,
    db: AsyncSession = Depends(get_db),
    user_id: UUID = Depends(get_current_user_id),
):
    return await ProjectService.update_project(
        db=db, user_id=user_id, project_id=project_id, data=data
    )


@router.delete("/{project_id}", status_code=status.HTTP_204_NO_CONTENT)
async def delete_project(
    project_id: UUID,
    db: AsyncSession = Depends(get_db),
    user_id: UUID = Depends(get_current_user_id),
):
    await ProjectService.delete_project(db=db, user_id=user_id, project_id=project_id)
```

### 6.2 修改 `backend/app/api/v1/conversations.py`

**`get_conversations`** 增加 `project_id` 查询参数：

```python
@router.get("", response_model=Page[ConversationResponse])
async def get_conversations(
    page: int = Query(1, ge=1),
    page_size: int = Query(10, ge=1, le=100, alias="pageSize"),
    keyword: Optional[str] = None,
    project_id: Optional[UUID] = Query(None, alias="projectId"),  # 新增
    db: AsyncSession = Depends(get_db),
    user_id: UUID = Depends(get_current_user_id),
):
    return await ConversationService.list_conversations(
        db=db, user_id=user_id, page=page, page_size=page_size,
        keyword=keyword, project_id=project_id,
    )
```

### 6.3 注册路由 `backend/app/api/router.py`

```python
from app.api.v1.projects import router as projects_router  # 新增

# 在现有 include_router 之后添加：
api_router.include_router(projects_router, prefix="/v1/projects", tags=["projects"])
```

---

## 七、API 端点总览

| 方法 | 路径 | 认证 | 说明 |
|------|------|------|------|
| `POST` | `/api/v1/projects` | 需要 | 创建项目 |
| `GET` | `/api/v1/projects` | 需要 | 列出用户的所有项目（含对话数量） |
| `GET` | `/api/v1/projects/:id` | 需要 | 获取单个项目详情 |
| `PATCH` | `/api/v1/projects/:id` | 需要 | 更新项目 |
| `DELETE` | `/api/v1/projects/:id` | 需要 | 删除项目（对话保留，project_id 置空） |
| `GET` | `/api/v1/conversations?projectId=xxx` | 需要 | 按项目过滤对话（已有端点新增参数） |
| `POST` | `/api/v1/conversations` | 需要 | 创建对话时可传 `projectId`（已有端点新增字段） |

### 请求/响应示例

**POST /api/v1/projects** 请求：
```json
{
  "name": "React Dashboard",
  "description": "前端管理后台",
  "defaultAgentIds": ["00000000-0000-0000-0000-000000000001"]
}
```
（字段名通过 `BaseSchema.to_camel` 自动转换，API 层面使用 camelCase。）

**GET /api/v1/projects** 响应：
```json
{
  "code": 200,
  "data": [
    {
      "id": "proj-uuid-001",
      "name": "React Dashboard",
      "description": "前端管理后台",
      "defaultAgentIds": ["00000000-0000-0000-0000-000000000001"],
      "conversationCount": 5,
      "createdAt": "2026-05-29T10:00:00Z",
      "updatedAt": "2026-05-29T10:00:00Z"
    }
  ],
  "message": "success"
}
```

**POST /api/v1/conversations** 请求（新增 projectId）：
```json
{
  "title": "实现登录页面",
  "type": "single",
  "agentIds": [],
  "projectId": "proj-uuid-001"
}
```

---

## 八、涉及文件清单

| 操作 | 文件 | 说明 |
|------|------|------|
| 新建 | `backend/alembic/versions/0005_add_projects.py` | 数据库迁移 |
| 新建 | `backend/app/models/project.py` | Project ORM 模型 |
| 修改 | `backend/app/models/__init__.py` | 注册 Project 模型 |
| 修改 | `backend/app/models/conversation.py` | 添加 project_id 字段 |
| 新建 | `backend/app/schemas/project.py` | Pydantic schemas |
| 修改 | `backend/app/schemas/conversation.py` | ConversationCreate/Response 加 projectId |
| 新建 | `backend/app/services/project.py` | 项目业务逻辑 |
| 修改 | `backend/app/services/conversation.py` | list/create/get 支持 project_id |
| 新建 | `backend/app/api/v1/projects.py` | 项目 API 路由 |
| 修改 | `backend/app/api/v1/conversations.py` | get_conversations 加 projectId 参数 |
| 修改 | `backend/app/api/router.py` | 注册 projects 路由 |

---

## 九、边界情况说明

1. **删除项目**：关联对话不删除，`project_id` 通过 FK `ON DELETE SET NULL` 自动置空
2. **`project_id` 合法性**：创建对话时若传入 `project_id`，由 FK 约束保证存在性；传了不存在的 ID 会触发数据库 IntegrityError（FastAPI 返回 500，可后续优化为 422）
3. **跨用户隔离**：所有项目操作都通过 `owner_id == user_id` 校验，用户 A 不能操作用户 B 的项目；对话同理，创建对话时 `project_id` 指向的项目必须属于当前用户（可由服务层额外校验或依赖 FK + owner 双重保障）
4. **存量兼容**：`project_id` 默认为 NULL，存量对话不归属任何项目，API 行为不变
5. **空项目列表**：`GET /projects` 返回空数组，不报错
