# AgentHub 前端 — 前后端对齐 P1/P2 实施计划 (2026-05-30)

## 实施目标

基于后端 6 个新提交 (e6b16ce..309978c)，完成前后端对齐。后端新增认证系统、项目管理、Agent 能力、编排器 DAG 等模块，前端需全链路补齐 API/类型/Hook/Store/Mock/UI。

## 计划实现功能

| 层级 | 模块 | 说明 |
|------|------|------|
| P1 T1-T6 | 后端对齐必需 | 对话项目关联、Agent 能力标签、认证 UI、密码修改、项目全链路、Mock 补齐 |
| P2 T7-T9 | 编排器/群聊 | DAG 可视化、SSE orchestrateMode、消息过滤参数 |

## 对齐文档

详见 `agenthub-web/docs/specs/2026-05-30-frontend-backend-alignment-gaps.md`

---

## 1. P1 — 后端对齐与体验补全

### T1 — 对话与项目关联

后端新增：
- `GET /conversations` 支持 `projectId` 查询过滤
- `POST /conversations` 支持 `project_id` 创建关联
- `ConversationResponse` 返回 `projectId`

前端改动：
- `types/chat.ts`：`Conversation` / `CreateConversationParams` 加 `projectId`
- `types/api.ts`：`ConversationListParams` 加 `projectId`
- `lib/api.ts`：`conversationApi.create` 透传 `projectId`；`list` 透传 `projectId`
- `hooks/useConversations.ts`：新增 `useConversationsByProject(projectId?)`
- `mocks/data.ts`：mock Conversation 加 `projectId`
- `mocks/handlers.ts`：GET 支持 `projectId` 过滤 + POST 存储 `projectId`

### T2 — Agent 能力标签

后端新增：
- `GET /v1/agents/capabilities` → `string[]`

前端改动：
- `lib/api.ts`：`agentApi.capabilities()`
- `hooks/useAgents.ts`：`useAgentCapabilities()`（staleTime=60s）
- `mocks/handlers.ts`：模拟 9 种能力标签

### T3 — 认证 UI 增强

前端改动：
- `components/layout/IconSidebar.tsx`：底部用户头像 + Dropdown（显示邮箱 + 退出登录）

### T4 — 密码修改 UI

后端新增：
- `PATCH /v1/auth/password`

前端改动：
- `lib/api.ts`：`authApi.changePassword(oldPassword, newPassword)`
- `components/settings/SettingsPage.tsx`：新增"安全"导航项 + 密码修改表单
- `mocks/handlers.ts`：模拟新旧密码相同拦截

### T5 — 项目管理全链路

后端新增：
- `POST /v1/projects` — 创建项目
- `GET /v1/projects` — 列出用户项目
- `GET /v1/projects/{id}` — 项目详情
- `PATCH /v1/projects/{id}` — 更新项目
- `DELETE /v1/projects/{id}` — 删除项目

前端新增：
- `types/project.ts`：`Project` / `CreateProjectParams` / `UpdateProjectParams`
- `types/api.ts`：6 个 API 类型别名
- `lib/api.ts`：`projectApi`（create/list/detail/update/delete）
- `hooks/useProjects.ts`：5 个 React Query hooks
- `stores/uiStore.ts`：`selectedProjectId`（localStorage 持久化）
- `components/project/ProjectSwitcher.tsx`：项目下拉选择器（IconSidebar 卡片入口）
- `components/project/ProjectCreateModal.tsx`：创建项目弹窗
- `components/layout/AppLayout.tsx`：按 `selectedProjectId` 过滤会话
- `mocks/data.ts`：2 个模拟项目
- `mocks/handlers.ts`：5 个 CRUD mock

### T6 — Mock 补齐

新增 Mock 端点：
- `POST /v1/auth/send-code` — 生成 6 位验证码，60 秒限频，10 分钟过期
- `POST /v1/auth/register` — 验证码校验 + 邮箱唯一 + 注册返回 Token
- `POST /v1/auth/login` — 邮箱密码校验返回 Token
- `POST /v1/auth/refresh` — 返回新 Token
- `GET /v1/auth/me` — 返回当前用户
- `GET /v1/orchestrator/tasks/{id}/dag` — 3 节点 2 边示例 DAG

预设账号：`test@agenthub.dev` / `123456`

---

## 2. P2 — 群聊与编排器

### T7 — DAG 可视化面板

后端新增：
- `GET /v1/orchestrator/tasks/{id}/dag` → `DagResponse { nodes[], edges[] }`

前端改动：
- `types/chat.ts`：新增 `DagNode` / `DagEdge` / `DagResponse`
- `lib/api.ts`：`orchestratorApi.dag(taskId)`
- `stores/chatStore.ts`：`dagTaskId` / `setDagTaskId`
- `components/chat/DagGraph.tsx`：SVG 拓扑图（分层布局 + 贝塞尔曲线连线 + 状态着色）
- `components/chat/ReActPanel.tsx`：双 Tab（ReAct / 任务图）切换

### T8 — SSE orchestrateMode 对接

后端变更：
- `GET /conversations/{id}/stream` 新增 `orchestrateMode` 参数
- SSE `agent_status` 事件包含 `task_id`

前端改动：
- `lib/sse.ts`：`createSSEStream` 新增 `orchestrateMode` 参数
- `components/layout/ChatArea.tsx`：群聊传 `auto_orchestrate`；`agent_status` 捕获 `task_id` → `dagTaskId`
- `mocks/sse.ts`：mock agent_status 新增 `task_id`

### T9 — 消息过滤参数

后端变更：
- `GET /conversations/{id}/messages` 新增 `senderType`/`senderId` 参数

前端改动：
- `lib/api.ts`：`messageApi.list` 新增可选 `senderType`/`senderId`
- `mocks/handlers.ts`：mock 支持 senderType/senderId 过滤

---

## 3. 体验修复（4 项）

| # | 问题 | 修复 |
|---|------|------|
| 1 | 设置页左侧导航跟随滚动 | SettingsPage 外层加 `minHeight: 0` |
| 2 | 项目管理 UI 隐蔽、风格不统一 | ProjectSwitcher 移至 IconSidebar 中间，卡片式入口 |
| 3 | 返回主页弹新建对话 | `resetNewConvTrigger()` 消费后清零 |
| 4 | 无用户信息编辑 | SettingsPage "个人信息"区域 + `authApi.updateProfile()` + Mock |

## 4. 后端待实现（1 项）

- `PATCH /v1/auth/me` — 用户信息编辑接口。前端已就绪，详见对齐文档。
